# Throwaway probe for #128. Never triggers; exists only so the dependency
# graph gains an Action version with a published advisory, to show the
# dependency-review job reds rather than passing over it. Reverted in the
# next commit.
name: zz-probe-delete-me
on: workflow_dispatch
permissions: {}
jobs:
  never:
    runs-on: ubuntu-latest
    if: false
    steps:
      - uses: Tiryoh/actions-mkdocs@v0.24.0
